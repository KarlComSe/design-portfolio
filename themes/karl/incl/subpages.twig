<div class="subnav">
    <p>Undersidor till denna sida:</p>
    <div id="subnav">
    <ul>
        {% for page in pages(current_page.id) if page.title and not page.hidden %}
            <li>
                <a href="{{ page.url }}">{{ page.title }}</a>
            </li>
        {% endfor %}
    </ul>
</div>
</div>